#include <stdio.h>

//二分查找
#define MAX_SIZE 6

int bin_search(int *a,int length, int key)
{
    int mid = length/2;
    int low = 0;
    int high = length;
    while(low <= high && high != 0)
    {
        
    }
    return -1;
}

int main()
{
    int a[MAX_SIZE]={1,3,4,6,8,9};
    printf("%d\n",a[6]);
    return 0;
}